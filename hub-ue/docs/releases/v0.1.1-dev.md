# v0.1.1-dev

Baseline de desenvolvimento para instalação e diagnóstico replicáveis do hub/plugin.

## Validado

- `biofeedback-doctor` instalado como CLI do hub.
- Diagnóstico de hub online/offline via `/health` e `/status`.
- Destaque de clientes Unreal inscritos em `unreal.commands`.
- Script de instalação do plugin em projetos Unreal consumidores.
- Script de checagem de configuração do plugin em projetos Unreal consumidores.
- Script de empacotamento limpo do plugin em pasta e ZIP versionados.
- Workflow `Release Unreal Plugin` para gerar o ZIP e anexar o asset em GitHub Releases.
- Templates `DefaultGame` para Editor e Meta Quest.
- Fluxo manual revisado no projeto externo `TesteVR`.

## Compatibilidade conhecida

- Herda a baseline `v0.1.0-dev` do hub Python e do plugin `QuestSupervisor`.
- Hub Python versionado como `0.1.1.dev0`.
- Plugin Unreal `QuestSupervisor` versionado como `0.1.1-dev`.
- Testado com o projeto externo `TesteVR` no caminho local de desenvolvimento.

## Limitações

- O workflow automatiza o pacote em tags futuras; releases antigas ainda podem ter assets anexados manualmente.
- O checker PowerShell é diagnóstico: ele não altera `DefaultGame.ini`.
- O versionamento ainda é manual entre hub, plugin, docs e tags.

## Referências

- Guia de integração: `docs/unreal-project-integration.md`.
- Templates de configuração: `docs/templates/`.
- Registro da baseline anterior: `docs/releases/v0.1.0-dev.md`.
